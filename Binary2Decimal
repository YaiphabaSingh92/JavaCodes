public class project1
{
	public static void main(String[] arg)
	{
		int number=1100001,i=0,rem,sum=0;
	 
		do
		{
			rem=number%10;
			number=number/10;
			sum+=rem*(Math.pow(2,i));
			i++;
			
		} while (number!=0);
		System.out.println("Decimal value of the binary is "+sum);
			
	}
}
